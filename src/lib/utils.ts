// lib/utils.ts
export function isReputableSource(source: string): boolean {
    // Your implementation of isReputableSource
    const reputableSources = ['example.com', 'another.org'];
    return reputableSources.includes(source);
  }
  
